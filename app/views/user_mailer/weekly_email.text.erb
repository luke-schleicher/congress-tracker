Here's how your lawmakers have voted recently:

<% @user.legislators.each do |leg| %>

  <%= leg.name %>

  <% if leg.five_recent_votes.empty? %>
    No recent votes
  <% else %>
    <% leg.five_recent_votes.each do |vote| %>
      <%= vote.bill_id %>
      <%= vote.vote_type %>
    <% end %>
  <% end %>

<% end %>
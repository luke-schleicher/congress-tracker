Here's how your lawmakers voted this week:

<% @user.legislators.each do |leg| %>

  <%= leg.name %>

  <% if leg.five_recent_votes.any? %>

    <% leg.five_recent_votes.each do |vote| %>

      <%= vote.bill_id %>: <%= vote.type %>

    <% end %>

  <% else %>

    No recent votes

  <% end %>

<% end %>